#!/usr/bin/env python
# -*- coding: utf-8 -*-

import time

def execute(robot):
    try:
        counter = '0'
        while True:
            print(len(l))
            l.insert(0, counter)
            print(l[0])
            l.append('thing')
            l.pop(1)
            counter = counter + 1
        
    except KeyboardInterrupt:
        raise
